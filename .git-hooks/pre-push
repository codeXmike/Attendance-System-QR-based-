
# branch="$(git rev-parse --abbrev-ref HEAD)"

# if [ "$branch" = "dev" ] || [ "$branch" = "main" ]; then
#   echo "🚫 Direct pushes to '$branch' are blocked!"
#   echo "👉 Please push to a feature branch and open a Pull Request into '$branch'."
#   exit 1
# fi
#!/bin/sh
pwsh -File .git-hooks/pre-push.ps1
